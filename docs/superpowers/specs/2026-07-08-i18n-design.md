# Picot 国际化 (i18n) 设计文档

> **日期**: 2026-07-08
> **状态**: 设计已批准（含 code review 修订），待实施
> **作者**: Dr. Lin & Mr. Spock

## 1. 目标

为 Picot 添加国际化支持，首期支持**中文**和**英文**，架构上预留扩展其他语言的能力。

**核心需求：**
1. 所有页面的用户可见文本支持 i18n
2. Settings → General 新增"Language"设置，允许用户手动选择：System Default / 中文 / English
3. 语言偏好跨 workspace 窗口共享（每个 workspace 运行在不同 `localhost:port`）
4. 语言切换后**即时生效**：静态 DOM 文本和已渲染的动态 JS 组件文本都要更新

## 2. 约束

| 约束 | 说明 |
|------|------|
| 纯原生 JS，无框架 | i18n 引擎必须零依赖、零构建步骤 |
| 无前端构建管线 | ES modules 直接以 `<script type=module>` 加载，不能引入打包工具 |
| Cookie 持久化（非 localStorage） | 每个 workspace 是不同端口的 `localhost:300X`，localStorage 按端口隔离。语言偏好必须跨端口共享——与 `themes.js` 完全同样的约束和模式 |
| Tauri v2 桌面应用 | 非 Web 部署，无 SSR |
| `cost.html` 在 iframe 中加载 | 同源同端口，共享 cookie，但 iframe 无法监听父页面 cookie 变化，需 postMessage 同步 |
| `bootstrap.html` 是独立错误页 | 纯静态 `<script>`，无 ES module，需特殊处理 |
| Picot 大量文本是 JS 运行时动态生成 | `applyTranslations()` 只处理当前 DOM 上已有的 `data-i18n` 属性节点；JS 生成的文本需 locale-change 事件驱动重新渲染 |

## 3. 方案选择

**选定方案：自建轻量 i18n 模块**（放弃 i18next / 多份 HTML 预渲染方案）

**理由：**
- Picot 的 i18n 需求极简：~500 个扁平字符串 + `{var}` 插值，不需要复数、延迟加载、命名空间等高级功能
- 自建引擎 ~200 行代码，与 `themes.js` 的 cookie 模式完全对称
- 零依赖、零构建，符合项目核心约束
- 后续加语言只需加一个 JSON 文件

## 4. 架构设计

### 4.1 文件结构

```
public/
  i18n.js                      ← 新模块：i18n 引擎（核心）
  locales/
    en.json                    ← 英文翻译（source of truth + fallback）
    zh.json                    ← 中文翻译
  index.html                   ← 静态文本加 data-i18n 属性
  cost.html                    ← 静态文本加 data-i18n 属性（Phase 2）
  bootstrap.html               ← 内联翻译逻辑
  app.js                       ← 引入 i18n，await initI18n()，动态文本改 t() 调用
  各模块 .js                    ← 动态文本改 t() 调用，订阅 onLocaleChange
  app-voice-input.js           ← recognition.lang 跟随 locale
```

### 4.2 i18n 引擎设计 (`public/i18n.js`)

#### 导出 API

```javascript
// 初始化：加载 en.json（fallback）+ 当前 locale JSON，扫描 DOM
export async function initI18n(): Promise<void>;

// 翻译函数：支持 {var} 插值，英文 fallback
export function t(key: string, params?: Record<string, string|number>): string;

// 设置语言：写 cookie，重新加载 locale，重新扫描 DOM，广播 locale-change 事件
export async function setLocale(preference: string): Promise<void>;

// 获取当前有效语言代码（"en" | "zh"）
export function getLocale(): string;

// 获取用户选择（"system" | "en" | "zh"），用于设置面板高亮
export function getLanguagePreference(): string;

// 订阅 locale 变化：动态组件注册回调，在语言切换时重新渲染
export function onLocaleChange(listener: (detail: { locale: string; preference: string }) => void): () => void;

// 语言选项常量（设置面板用）
export const LANGUAGES: Array<{ value: string; labelKey?: string; nativeLabel?: string }>;

// 纯函数：从偏好解析 locale，供测试注入
export function resolveLocale(preference: string, systemLanguage?: string): string;
```

#### 核心逻辑

```
initI18n()  ← await，阻塞主 app 初始化
  ├── preference = readCookie("picot-language") ?? "system"
  ├── locale = resolveLocale(preference, navigator.language)
  │     ├── preference === "system" → navigator.language 前缀匹配
  │     │     "zh*" → "zh", 其余 → "en"
  │     └── preference === "en" | "zh" → 直接使用
  ├── enMessages = await fetchLocale("en")        ← 始终加载英文作为 fallback
  ├── activeMessages = locale === "en"
  │     ? enMessages
  │     : await fetchLocale(locale)
  ├── document.documentElement.lang = BCP47_TAG[locale]
  └── applyTranslations(document)
```

```
setLocale(preference)
  ├── writeLanguageCookie(preference)
  ├── locale = resolveLocale(preference, navigator.language)
  ├── activeMessages = locale === "en"
  │     ? enMessages
  │     : await fetchLocale(locale)
  ├── document.documentElement.lang = BCP47_TAG[locale]
  ├── applyTranslations(document)  ← 更新静态 DOM data-i18n 节点
  └── broadcastLocaleChange(locale, preference)
        ├── 遍历 onLocaleChange 注册的 listeners，调用每个回调
        └── window.dispatchEvent(new CustomEvent("picot:locale-change", { detail }))
```

```
t(key, params)
  ├── translated = lookup(activeMessages, key)
  ├── if translated != null → interpolate({var}) → 返回
  ├── fallback = lookup(enMessages, key)
  ├── if fallback != null → interpolate({var}) → 返回
  ├── console.warn(`[i18n] Missing key: ${key}`)
  └── 返回 key 本身
```

#### 关键设计决策

**1. 始终加载 en.json 作为 fallback**

```javascript
let enMessages = {};      // 英文 messages，初始化时加载，永不卸载
let activeMessages = {};  // 当前 locale 的 messages

// t() 查找顺序：activeMessages → enMessages → key 本身
```

zh.json 缺某 key 时，`t()` 从 `enMessages` 取英文。这兑现了风险表中"翻译遗漏时 fallback 到英文"的承诺。

**2. 阻塞主 app 初始化**

```javascript
// app.js 初始化区域

// Restore saved theme
const savedTheme = getCurrentTheme();
applyTheme(savedTheme);

// Initialize i18n — 阻塞直到 locale 加载完成
await initI18n();

// 以下初始化在 locale ready 后执行，t() 调用拿到正确翻译
setupContextViz(...);
setupVoiceInput(...);
buildThemeGrid();
buildLanguageSelector();
```

Tauri v2 桌面 WebView 支持 ES module top-level await。locale JSON < 15KB，阻塞几十毫秒比混合语言状态安全。不使用 top-level await 时可包一层 `async function main() { ... } main();`。

**3. Locale-change 事件驱动动态组件重渲染**

`setLocale()` 广播事件后，各动态组件的回调负责重新渲染自身文本。这是"即时切换"成立的核心机制：

| 组件 | onLocaleChange 回调 |
|------|-------------------|
| `buildLanguageSelector()` | 重建选项列表（System Default label 跟随 locale） |
| `SessionSidebar` | 重新渲染当前列表（`render()` 或 `loadSessions({ quiet: true })`），保留滚动位置 |
| `MessageRenderer` | 若 welcome 可见则重新渲染 welcome；thinking labels 更新 |
| `FileBrowser` | 若当前有 loading/empty/error 文本则重绘；文件名不翻译 |
| `FolderPicker` | 若弹窗打开则重绘文本 |
| `app-updater` | 重新应用当前 updater status（"Checking..."、"Download & install" 等） |
| `app-voice-input` | 更新 `recognition.lang`（只影响下次录音，不中断当前录音） |
| `app-settings-editors` | 若 API key panel 打开则重绘 loading/error 文本 |
| `DialogHandler` | 若对话框打开则更新按钮文本 |
| `ToolCardRenderer` | 新渲染的 tool cards 自动用新 locale；已渲染的 complete/error 标签在下次渲染时更新 |

**4. `data-i18n-html` 不在 Phase 1 支持**

Phase 1 只支持四个属性，全部用 `textContent` / `placeholder` / `title` / `aria-label` 替换。需要 HTML 结构的复杂文本（如 settings help 段落中的 `<code>` 和 `<a>` 标签）用拆分 DOM 节点解决：

```html
<!-- 不用 data-i18n-html -->
<p class="settings-help">
  <span data-i18n="settings.models.prefix">Edit</span>
  <code>~/.pi/agent/models.json</code>
  <span data-i18n="settings.models.middle">to add custom providers</span>
  <a href="..." data-i18n="settings.models.docs">models.json docs</a>
  <span data-i18n="settings.models.suffix">for the full schema.</span>
</p>
```

如未来确需 `data-i18n-html`，必须加白名单 sanitizer + code review 逐个确认。

#### Cookie 策略

与 `themes.js` 完全一致的实现模式（encode/decode、SameSite=Lax、try/catch、Biome ignore）：

```javascript
const LANG_COOKIE = "picot-language";
const LANG_COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 365 * 10; // 10 年

function readLanguageCookie() {
  try {
    const cookies = document.cookie ? document.cookie.split("; ") : [];
    for (const entry of cookies) {
      const eq = entry.indexOf("=");
      if (eq === -1) continue;
      if (entry.slice(0, eq) !== LANG_COOKIE) continue;
      const raw = entry.slice(eq + 1);
      try {
        return decodeURIComponent(raw);
      } catch {
        return raw;
      }
    }
  } catch {
    // document.cookie can throw in sandboxed contexts; treat as missing.
  }
  return null;
}

function writeLanguageCookie(preference) {
  try {
    const value = encodeURIComponent(preference);
    // biome-ignore lint/suspicious/noDocumentCookie: Cookie Store API is async and not suitable for synchronous cross-port language persistence
    document.cookie = `${LANG_COOKIE}=${value}; Max-Age=${LANG_COOKIE_MAX_AGE_SECONDS}; Path=/; SameSite=Lax`;
  } catch {
    // ignore — same fallback as the read path
  }
}
```

Cookie 值存的是用户偏好（`"system"` / `"en"` / `"zh"`），而非解析后的 locale。当系统语言改变时，"System Default" 用户在重新打开窗口时会自动跟随。

#### BCP-47 语言标签

DOM `lang` 属性使用完整 BCP-47 标签，对屏幕阅读器和拼写检查更友好：

```javascript
const BCP47_TAG = {
  en: "en",
  zh: "zh-CN",
};
document.documentElement.lang = BCP47_TAG[locale];
```

#### Locale JSON 结构

使用嵌套对象，按功能模块分组。以下是 Phase 1 范围的示例（非完整列表）：

```json
{
  "app": {
    "name": "Picot",
    "welcome": "Welcome to Picot",
    "welcomeHint": "Type a message below to start chatting with Pi, or select a session from the sidebar."
  },
  "sidebar": {
    "settings": "Settings",
    "search": "Search...",
    "clearSearch": "Clear search",
    "openFolder": "Open folder as workspace",
    "refreshSessions": "Refresh sessions",
    "loadingSessions": "Loading sessions...",
    "update": "Update",
    "showMore": "Show more",
    "showLess": "Show less",
    "favourites": "Favourites",
    "archived": "Archived"
  },
  "composer": {
    "placeholder": "Type a message...",
    "attachImage": "Attach image",
    "commands": "Commands",
    "switchModel": "Switch model",
    "thinkingOff": "Think off",
    "voiceInput": "Voice input",
    "sendMessage": "Send message",
    "abort": "Abort (Esc)",
    "waitingForFinish": "Waiting for current session to finish…"
  },
  "settings": {
    "title": "Settings",
    "general": "General",
    "extensions": "Extensions",
    "usage": "Usage",
    "configuration": "Configuration",
    "backToChat": "Back to chat",
    "appearance": "Appearance",
    "language": {
      "title": "Language",
      "systemDefault": "System default",
      "description": "Choose the interface language. Changes apply immediately."
    },
    "agent": "Agent",
    "autoCompaction": "Auto-compaction",
    "thinkingEffort": "Thinking effort",
    "reasoningDepth": "Reasoning depth",
    "showThinking": "Show thinking",
    "updates": "Updates",
    "piVersion": "Pi version",
    "picotVersion": "Picot version",
    "checkForUpdates": "Check for updates",
    "checkNow": "Check now",
    "status": "Status",
    "updateAvailable": "Update available",
    "downloadInstall": "Download & install"
  },
  "status": {
    "connecting": "Connecting...",
    "connected": "Connected",
    "done": "Done",
    "failed": "Failed",
    "error": "Error",
    "compacting": "Compacting context…",
    "compacted": "Context compacted",
    "abortedByUser": "Aborted by user",
    "connectionLost": "Connection lost. Please refresh the page."
  },
  "errors": {
    "nativeHostRequired": "{action} is only supported with a native host.",
    "failedToStart": "Failed to start {what}: {error}",
    "failedToSwitch": "Failed to switch session: {error}",
    "failedToLoad": "Failed to load {what}: {error}",
    "installFailed": "Install failed",
    "uninstallFailed": "Uninstall failed",
    "keyCannotBeEmpty": "Key cannot be empty.",
    "save": "Save",
    "saving": "Saving...",
    "saved": "Saved",
    "cancel": "Cancel",
    "remove": "Remove",
    "retry": "Retry"
  },
  "files": {
    "title": "Files",
    "parentDir": "Parent directory",
    "openInManager": "Open in file manager",
    "close": "Close",
    "loading": "Loading…",
    "empty": "Empty directory",
    "failedLoad": "Failed to load"
  },
  "messages": {
    "new": "New",
    "queued": "Queued",
    "cancel": "Cancel",
    "copy": "Copy",
    "copied": "Copied!",
    "copyMessage": "Copy message",
    "complete": "complete",
    "error": "error"
  },
  "onboarding": {
    "openProject": "Open a project to start chatting.",
    "configureKey": "Configure an API key or provider to start chatting."
  },
  "bootstrap": {
    "couldNotStart": "Picot could not start",
    "embeddedPiFailed": "The embedded pi runtime that ships with Picot failed to launch.",
    "reinstallHelp": "This usually means the installation is incomplete or corrupted. Reinstalling Picot normally fixes it. Click Retry startup after reinstalling or rebooting.",
    "retryStartup": "Retry startup",
    "startingPi": "Starting Pi runtime...",
    "tauriUnavailable": "Tauri runtime is unavailable in this window.",
    "startupFailed": "Startup failed"
  }
}
```

### 4.3 Key 命名规范

- **点分嵌套**，按模块/功能区域分组：`sidebar.settings`、`composer.placeholder`
- 第一级对应模块名（`sidebar`、`composer`、`settings`、`files`、`status`、`errors`、`messages`、`onboarding`、`bootstrap`）
- 避免超过 3 层嵌套
- 错误消息统一在 `errors.*` 下，带 `{var}` 插值参数

### 4.4 HTML 标注约定

| 属性 | 替换目标 | 示例 |
|------|---------|------|
| `data-i18n="key"` | `textContent` | `<span data-i18n="sidebar.settings">Settings</span>` |
| `data-i18n-ph="key"` | `placeholder` | `<input data-i18n-ph="sidebar.search" placeholder="Search...">` |
| `data-i18n-title="key"` | `title` | `<button data-i18n-title="sidebar.openFolder" title="Open folder">` |
| `data-i18n-aria-label="key"` | `aria-label` | `<button data-i18n-aria-label="sidebar.refresh" aria-label="Refresh">` |

**原则**：HTML 中保留英文原文作为 fallback（在 i18n 初始化前的极短时间内可见），避免空文本闪烁。

**不支持 `data-i18n-html`**（Phase 1）。需要 HTML 结构的复杂文本用拆分 DOM 节点解决（见 4.2 关键设计决策 #4）。

### 4.5 语言选择器 UI

在 Settings → General → Appearance 下方新增 **Language** section：

```html
<div class="settings-section">
  <div class="settings-section-title" data-i18n="settings.language.title">Language</div>
  <div class="settings-language-options" id="settings-language-options">
    <!-- 由 JS 动态生成 -->
  </div>
  <p class="settings-help" data-i18n="settings.language.description">
    Choose the interface language. Changes apply immediately.
  </p>
</div>
```

语言选项常量：

```javascript
const LANGUAGES = [
  { value: "system", labelKey: "settings.language.systemDefault" },
  { value: "en", nativeLabel: "English" },
  { value: "zh", nativeLabel: "中文" },
];
```

渲染逻辑（参考 `buildThemeGrid()` 的模式）：

```javascript
function buildLanguageSelector() {
  const container = document.getElementById("settings-language-options");
  const current = getLanguagePreference();
  container.innerHTML = "";
  for (const lang of LANGUAGES) {
    const label = document.createElement("label");
    label.className = "settings-language-option";
    const radio = document.createElement("input");
    radio.type = "radio";
    radio.name = "language";
    radio.value = lang.value;
    radio.checked = current === lang.value;
    radio.addEventListener("change", () => setLocale(lang.value));
    const span = document.createElement("span");
    // 语言名称用本语种显示（English / 中文）；System Default 跟随 UI 翻译
    span.textContent = lang.nativeLabel ?? t(lang.labelKey);
    label.appendChild(radio);
    label.appendChild(span);
    container.appendChild(label);
  }
}
```

**设计决策**：
- 语言名称（English / 中文）始终用本语种显示——用户需要认出自己要选的语言
- "System default" 跟随当前 UI 语言翻译，否则中文界面里唯一英文残留就是它

### 4.6 初始化时序

`app.js` 中的初始化顺序，`await initI18n()` 放在 `applyTheme()` 之后、其他 UI 初始化之前：

```javascript
// app.js 初始化区域（约 line 3552）

// Restore saved theme
const savedTheme = getCurrentTheme();
applyTheme(savedTheme);

// Initialize i18n — 阻塞直到 locale 加载完成
await initI18n();

// 以下初始化在 locale ready 后执行
setupContextViz({ ... });
setupVoiceInput({ ... });
buildThemeGrid();
buildLanguageSelector();
```

Tauri v2 桌面 WebView 支持 ES module top-level await。locale JSON < 15KB，阻塞几十毫秒可接受。若不想用 top-level await，包一层 `async function main() { ... } main();`。

### 4.7 动态文本改造模式

模块中的 `innerHTML` / `textContent` 硬编码字符串改为 `t()` 调用：

```javascript
// 改造前
this.container.innerHTML = '<div class="file-loading">Loading…</div>';

// 改造后
import { t } from "./i18n.js";
this.container.innerHTML = `<div class="file-loading">${t("files.loading")}</div>`;
```

**带插值的改造**：

```javascript
// 改造前
renderError(`Failed to start new session: ${e}`);

// 改造后
renderError(t("errors.failedToStart", { what: "session", error: String(e) }));
// en.json: "errors": { "failedToStart": "Failed to start {what}: {error}" }
// zh.json: "errors": { "failedToStart": "启动{what}失败：{error}" }
```

**底层错误边界**：`transport.js` 等底层模块的协议错误不翻译。上层展示时用 `t()` 包裹用户可见的前缀文本，原始 error message 保留英文作为 `{error}` 插值。

### 4.8 onLocaleChange 订阅模式

动态组件在初始化时注册 locale-change 回调：

```javascript
// session-sidebar.js 示例
import { onLocaleChange } from "./i18n.js";

export class SessionSidebar {
  constructor(...) {
    // ...
    onLocaleChange(() => {
      // 重新渲染当前列表，保留滚动位置和展开状态
      this.rerenderLabels();
    });
  }

  rerenderLabels() {
    // 重新渲染 show more/less、favourites、archived 等标签
    // 不重新 fetch sessions，只更新文本
    if (this.container.children.length > 0) {
      this.render(this.projects, this.sessions);
    }
  }
}
```

```javascript
// app-voice-input.js 示例
import { getLocale, onLocaleChange } from "./i18n.js";

export function setupVoiceInput({ micBtn, messageInput }) {
  // ...
  function applyRecognitionLocale() {
    recognition.lang = getLocale() === "zh" ? "zh-CN" : "en-AU";
  }
  applyRecognitionLocale();
  onLocaleChange(applyRecognitionLocale);
  // 注：正在录音时不 restart recognition，只影响下次录音
}
```

### 4.9 完整模块改造清单

以下清单基于代码审计（grep `textContent`/`innerHTML`/`placeholder`/`title`/`aria-label`/`renderError`），不是凭印象列模块。

#### Phase 1：基础设施 + 核心聊天 UI + Bootstrap

| 文件 | 改造方式 | 用户可见文本示例 |
|------|---------|----------------|
| `i18n.js` (新建) | 引擎模块 | — |
| `locales/en.json` (新建) | 英文 source of truth | — |
| `locales/zh.json` (新建) | 中文翻译 | — |
| `index.html` | 静态文本加 `data-i18n`/`-ph`/`-title`/`-aria-label` | ~80 串 |
| `app.js` | `await initI18n()`，动态文本改 `t()`，`onLocaleChange` 注册 | welcome、status、compaction、error 消息、placeholder |
| `session-sidebar.js` | `t()` + `onLocaleChange` | loading、retry、show more/less、favourites、archived、delete dialog |
| `message-renderer.js` | `t()` + `onLocaleChange` | welcome、copy message、thinking label、error |
| `tool-card.js` | `t()` | complete、error、copy output |
| `file-browser.js` | `t()` + `onLocaleChange` | Loading、Empty directory、Failed to load |
| `folder-picker.js` | `t()` + `onLocaleChange` | Open Folder、Loading、No subdirectories、Failed to load |
| `dialogs.js` | `t()` + `onLocaleChange` | Select an option、Confirm、Input、Editor、Cancel |
| `markdown.js` | `t()` | Copy、Copied! |
| `app-settings-editors.js` | `t()` + `onLocaleChange` | Loading providers、Save、Cancel、Remove、Retry、No providers known |
| `app-settings-toggles.js` | `t()` | Thinking: {level} |
| `app-updater.js` | `t()` + `onLocaleChange` | Checking、Download & install、Retry、Installing、unknown、Update |
| `app-context-viz.js` | `t()` | {pct}% used、Context Window |
| `app-voice-input.js` | `getLocale()` + `onLocaleChange` | recognition.lang |
| `workspace-actions.js` | `t()` | 12+ renderError 调用（native host required、failed to start/switch/open） |
| `package-install-status.js` | `t()` | Install failed、Uninstall failed、npm missing explanation |
| `settings-save-status.js` | `t()` | Saving...、Save、Saved |
| `onboarding-state.js` | `t()` | Open a project to start chatting、Configure an API key |
| `bootstrap.html` | 内联 cookie 读取 + 翻译 | 8 串（Picot could not start、Retry startup 等） |
| `style.css` | 新增语言选择器样式 | — |

#### 明确不改造的文件

| 文件 | 原因 |
|------|------|
| `transport.js` | 底层协议模块，错误经上层 `renderError` 展示时用 `t()` 包裹前缀 |
| `image-attachments.js` | 只有 `throw new Error()` 开发者错误，非用户可见 |
| `sidebar-search-control.js` | 无用户可见文本，只做事件 wiring；placeholder 在 HTML 上标注 |
| `state.js` | 状态管理，无 UI 文本 |
| `session-routing.js` | 路由工具，无 UI 文本 |
| `layout-insets.js` | 布局计算，无 UI 文本 |
| `history-scroll-anchor.js` | 滚动锚点，无 UI 文本 |
| `new-session-refresh.js` | 文件解析，无 UI 文本 |
| `package-install-status.js` 的 `summarizePackageError()` | 原始错误信息保留英文，不翻译 |

#### Phase 2：Cost 仪表盘

| 文件 | 改造方式 |
|------|---------|
| `cost.html` | 静态文本加 `data-i18n` |
| `cost.js` | `await initI18n()` + postMessage 监听 |
| `cost-infobar.js` | `t()` + `onLocaleChange` |

### 4.10 "System Default" 语义

- "System Default" 在**每次窗口初始化**时解析 `navigator.language`：`"zh*"` 前缀 → `"zh"`，其余 → `"en"`
- **不承诺** app 运行中监听 OS language change 事件（WebView 不可靠触发 `navigator.language` 变化）
- 若用户保持窗口打开并修改系统语言，需重新打开窗口或重新选择 "System Default" 才生效
- 设置面板文案：

> System Default resolves from your system language at app startup. Runtime OS language changes are picked up after reopening the window.

### 4.11 Cost 页面（iframe）i18n（Phase 2）

`cost.html` 在 iframe 中加载，同源同端口，共享 cookie。初始化时调用 `await initI18n()` 即可。但父页面 Settings 中切换语言时，iframe 无法监听 cookie 变化（浏览器无可靠 cookie observer）。

**同步协议**：父页面 `setLocale()` 后通过 `postMessage` 通知 iframe：

```javascript
// app.js 中 setLocale 完成后
const costIframe = document.querySelector(".settings-usage-iframe");
costIframe?.contentWindow?.postMessage(
  { type: "picot:locale-change", locale: getLocale(), preference: getLanguagePreference() },
  window.location.origin,
);
```

```javascript
// cost.js
window.addEventListener("message", (event) => {
  if (event.origin !== window.location.origin) return;
  if (event.data?.type === "picot:locale-change") {
    initI18n(); // 重新加载 locale + 重新扫描 DOM
  }
});
```

### 4.12 Bootstrap 错误页 i18n

`bootstrap.html` 是独立错误页，使用内联 `<script>`（非 ES module），只有约 8 个字符串。读取 cookie，用内联 JS 替换文本。简单的 `if (lang === "zh")` 分支即可，不值得引入完整 i18n 引擎。

```javascript
// bootstrap.html 内联 script
const lang = (document.cookie.match(/picot-language=([^;]+)/) || [])[1];
const isZh = lang === "zh" || (!lang && navigator.language.startsWith("zh"));
const messages = isZh ? zhMessages : enMessages;
// 替换各元素文本
```

8 个字符串的翻译直接内联在 script 中，不单独建 locale JSON。

## 5. 分阶段实施

### Phase 1：基础设施 + 核心聊天 UI + Bootstrap

**目标**：i18n 引擎 + 核心聊天体验全中文化 + 错误页中文化

1. 创建 `public/i18n.js` 引擎模块（含 `onLocaleChange` 事件机制）
2. 创建 `public/locales/en.json` 和 `public/locales/zh.json`（Phase 1 范围）
3. `index.html` 加 `data-i18n` 属性标注
4. `app.js` 添加 `await initI18n()`，添加语言选择器
5. 改造 Phase 1 清单中的所有模块（22 个文件，见 4.9 表）
6. `bootstrap.html` 内联翻译
7. 新增 CSS（语言选择器样式）
8. 单元测试：`i18n.test.js`

### Phase 2：Cost 仪表盘

1. `cost.html` / `cost.js` / `cost-infobar.js` i18n 化
2. postMessage 同步协议
3. 补充 locale JSON 中 Phase 2 的 keys

## 6. 测试策略

### 单元测试 (`public/i18n.test.js`)

| 测试用例 | 说明 |
|---------|------|
| `t()` 基本翻译 | key 存在时返回正确翻译 |
| `t()` 插值 | `{var}` 占位符正确替换 |
| `t()` missing key | 未找到 key 时返回 key 本身 + console.warn |
| `t()` 英文 fallback | `activeMessages` 缺 key 时从 `enMessages` fallback |
| `t()` 英文也缺 | 返回 key 本身 |
| `resolveLocale("system", "zh-CN")` | → "zh" |
| `resolveLocale("system", "en-US")` | → "en" |
| `resolveLocale("zh")` | → "zh"（忽略 systemLanguage 参数） |
| `getLanguagePreference()` 无 cookie | → "system" |
| `setLocale("zh")` 写 cookie | cookie 值为 "zh"，encoded |
| `setLocale("zh")` 广播事件 | `onLocaleChange` 回调被调用，`CustomEvent` 被 dispatch |
| `applyTranslations()` DOM 扫描 | `data-i18n`/`-ph`/`-title`/`-aria-label` 元素被替换 |
| `initI18n()` 加载 en + zh | 两个 locale JSON 都被 fetch |

### Key 完整性测试

| 测试用例 | 说明 |
|---------|------|
| zh.json key 集合 ⊇ en.json key 集合 | 不允许 zh 缺 key（可有多余 key，但 warn） |
| locale JSON value 类型 | 必须是 string 或嵌套 object，不允许 null/number |
| locale JSON 不允许空字符串 | 除非显式 allowlist |
| HTML `data-i18n*` 属性引用的 key 存在于 en.json | 静态扫描 `index.html`、`cost.html` |
| JS `t("literal.key")` key 存在于 en.json | 正则静态扫描 Phase 1 改造后的 `.js` 文件 |

### 集成验证

1. **语言切换即时生效**：在设置中切换语言，静态 DOM 文本 + 已渲染的 sidebar / messages / tool cards / file browser / updater 状态立即变化，无需刷新
2. **跨窗口一致性**：在 workspace A 切换语言，打开 workspace B 窗口，B 自动使用相同语言
3. **System Default**：设置为 "System Default"，重新打开窗口，语言跟随 OS（不承诺运行时自动跟随）
4. **cost 仪表盘**（Phase 2）：在 iframe 中的 cost 页面通过 postMessage 同步语言
5. **bootstrap 错误页**：手动触发错误页（删 pi binary），验证中文显示

## 7. 风险与缓解

| 风险 | 概率 | 缓解 |
|------|------|------|
| 翻译遗漏（某 key 只在 en.json 没有 zh.json） | 高 | `t()` 自动 fallback 到英文（`enMessages` 常驻）；CI key 完整性测试强制 |
| 动态文本遗漏 i18n 改造 | 中 | 逐模块 checklist（4.9 表），code review 对照 |
| locale 切换后动态组件未重渲染 | 高 | `onLocaleChange` 事件机制 + 每个动态组件必须订阅；集成验证检查 |
| 翻译质量 | 中 | 中文翻译由 Dr. Lin 审校 |
| `app.js` 122KB 改动冲突 | 中 | i18n 改动以 `t()` 替换为主，改动点小而散，降低合并冲突 |
| locale JSON 加载延迟 | 低 | `await initI18n()` 阻塞初始化，JSON < 15KB，毫秒级 |
| `onLocaleChange` 回调遗漏导致中英混杂 | 中 | 集成验证逐组件检查；code review 确认每个动态组件订阅 |

## 8. 非目标 (Non-Goals)

- **不做** RTL（从右到左）语言支持
- **不做** 日期/数字本地化格式（Picot 不显示这类内容）
- **不做** 自动翻译（所有翻译人工维护）
- **不做** 按需加载 locale 文件（两种语言总共 ~30KB，不值得懒加载）
- **不做** 服务端翻译（pi 的 RPC 响应不翻译——那是 pi 的职责，Picot 只管自己的 UI）
- **不做** `data-i18n-html`（Phase 1，需要 HTML 结构的文本用拆分 DOM 节点解决）
- **不做** 运行时 OS language change 监听（WebView 不可靠，重新打开窗口时跟随即可）
