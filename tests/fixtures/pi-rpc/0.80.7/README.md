# Pi 0.80.7 RPC contract fixture

Generated with `bun run smoke:pi-rpc --update`. The fixture records only protocol shape metadata;
model catalogs, workspace paths, prompts, and credentials are deliberately excluded.
